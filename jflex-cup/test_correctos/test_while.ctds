class test_maxcomdiv{

int maxcomdiv (int a, int b){
        int dividendo,divisor,resto;
	
	resto = 1;
	while (((((resto != 0))))){

		resto += dividendo % divisor;
		dividendo -= divisor;
		continue;
	}

        while resto != 0 { //puede q no tenga parentesis

		break;
	}

        while ( resto != 0 ) { 

		; //es una sentencia valida
	}
	return dividendo;
}

int main(){
    externinvk("printf", float , "%dMcd",  maxcomdiv(6,9 ) );
}

}
