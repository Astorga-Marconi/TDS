class test_retorno{

	/*  Retorna x luego de modificarla y despues restablecer su valor original */
	int id(int x ){
		  int i;
			int j;
			int z;
			i=100;
			j=0; 
			z=x;
	 
		   while (j<i){
		      int i;
					i=x; 
		      while ((x>0) || (x<0) ){
							if(x>0){
								x=x-1;		
							}
							else{
								x=x+1;
							}
		      }
		      while ((x<i) || (x>i)){
				if (x<i){
 					x= x+1;}
		      	else {
		          	if (x==x){
		          	    x=x-1+(j*x-j*x);
		              }
		         }

		      }
			  x=z;
		      j=j+1;  
		   }
		   return x;
	}

		int main(){
		  externinvk("printf", int , id(9));
	}

}
