// No anda
class Program {
	int inc(int x) {
		return x + 1;
	}
	void main() {
		int	y;
		y = inc(externinvk("get_int", int));
		if (y == 1) {
			externinvk("printf", void, "y==1\n");
		} else {
			externinvk("printf", void, "y==%d\n", y);
		}
	}
}